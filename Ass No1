#include<stdio.h>
#include<string.h>
struct bookinfo{
int id;
char name[20];
char author[20];
};
void insertion (struct bookinfo b[],int d);
void deletion(struct bookinfo b[],int d);
void modification (struct bookinfo b[]);

int main()
{

struct bookinfo b[20];
int i;
int d;
printf("enter no of books");
scanf("%d",
for(i=0;i<4;i++)
{

printf("enter the id");
scanf("%d",&b[i].id);
printf("enter the name");
scanf("%s",b[i].name);
printf("enter the author");
scanf("%s",b[i].author);
}

printf ("book details are ");
for(i=0;i<4;i++)
{

printf("id is %d",b[i].id);
printf("name  is %s",b[i].name);
printf("author is %s",b[i].author);
}

int ch;
printf("1.insertion\n2.deletion\n3.modification\n ");
printf("enter your choice");
scanf("%d",&ch);
switch(ch);
{
case 1:insertion(b);
break;
case 2:deletion(b);
break;
case 3:modification(b);
break;
default :printf("exiting program");
}
}while (ch!=3)
return 0; 
{
void insertion (struct bookinfo b[]);
int n;
int i;
printf("you want to add some book"):
scanf("%d",&n);
for(i=4;i>=n;i--)
{
b[i+1].id=b[i].id;
strcpy(b[i+1].name,b[i].name );
strcpy(b[i+1].author,b[i].author);
}

printf("enter the data :");
printf("enter the id");
scanf("%d",&b[n].id);
printf("enter the name");
scanf("%s",b[n].name);
printf("enter the author");
scanf("%s",b[n].author);
for(i=0;i<5;i++)

{

printf("%d",i);
printf("id is %d",b[i].id);
printf("name  is %s",b[i].name);
printf("author is %s",b[i].author);
}
}
void deletion(struct bookinfo b[]{
int c;
printf("enter the position");
scanf("%d",&c);
int i;
if (c<0||c>=5){
printf("invalid position");
}
else{
for(i=c;i<5-1;i++){
b[i]=b[i+1];
}
}
printf("update record after deletion\n");
for (i=0;i<4;i++){
printf("%d)",n);









return 0;
}
